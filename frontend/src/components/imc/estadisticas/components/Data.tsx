import { Card, CardHeader, CardTitle } from "@/components/ui/card"

const Data = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Proximamente...</CardTitle>
      </CardHeader>
    </Card>
  )
}

export default Data
