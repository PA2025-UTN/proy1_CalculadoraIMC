import { Button } from "@/components/ui/button"

const Register = () => {
  return (
    <div>
      <Button variant="secondary" className="cursor-pointer">Registrarse</Button>
    </div>
  )
}

export default Register
